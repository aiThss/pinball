# Pinball project rules

<!-- BEGIN:auto-gp-rule -->
## Auto push after every change

After completing ANY requested change (fix, feature, refactor, etc.), always
`git add`, `git commit`, and `git push` the result to the current branch,
unless the user explicitly asks not to. "gp" means git push.
<!-- END:auto-gp-rule -->
